version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_create_environment:
      - pip install uv
      - uv pip install -e ".[docs]"
sphinx:
  configuration: docs/conf.py
formats: all
